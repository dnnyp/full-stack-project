<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Team Name</th>
      <th>Season</th>
      <th>League Size</th>
      <th>Scoring Format</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>{{roster.id}}</td>
      <td>{{roster.name}}</td>
      <td>{{roster.season}}</td>
      <td>{{roster.league_size}}</td>
      <td>{{roster.scoring_format}}</td>
    </tr>
  </tbody>
</table>

<button class="delete-roster btn btn-danger" data-id="{{roster.id}}">Delete Roster</button>
